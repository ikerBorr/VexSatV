services:
  dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    user: "${UID:-1000}:${GID:-1000}"
    working_dir: /work
    volumes:
      - ..:/work
      - vexsatv-cache:/cache
    stdin_open: true
    tty: true

volumes:
  vexsatv-cache: